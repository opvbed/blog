import Image from 'next/image';

// First, let's create a placeholder image in public folder
const placeholderImage = (
  <div className="relative w-full h-48 bg-gray-200 rounded-lg animate-pulse" />
);
